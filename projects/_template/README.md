# <project-name>

<One-line description.>

A project in the [Claude-Cloud sandbox](../../README.md). Scaffolded from
`projects/_template/`.

## Run

```bash
pip install -r requirements.txt   # plus shared/requirements.txt if you use sandbox clients
python src/main.py
```

Keys (if any) come from the repo-root `.env` — see [`.env.example`](../../.env.example).
